<%= form_with(model: company_asset, local: true) do |form| %>
  <% if company_asset.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(company_asset.errors.count, "error") %> prohibited this company_asset from being saved:</h2>

      <ul>
      <% company_asset.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :count %>
    <%= form.number_field :count %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
